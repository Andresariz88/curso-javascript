function multiplicar(num1, num2) {
    let res = num1 * num2;
    return res;
}

document.write(multiplicar(5,5));

const multiplicar = function(num1, num2) {
    let res = num1 * num2;
    return res;
}

const multiplicar = (num1, num2) => {
    let res = num1 * num2;
    return res;
}
const multiplicar = (num1, num2) => num1 * num2; // esto ya se retorna